
 Feature: only test
   this is test about the cucumber with ruby.
   test for the correct write of the feature.

   Scenario: test cucumber sum two numbers
     Given generate two numbers.
     When sum the two numbers.
     Then the result should be display.

   Scenario: test cucumber rest two numbers
     Given generate two numbers.
     When rest the two numbers.
     Then the result should be display.

   Scenario: times of the two numbers
     Given generate two numbers.
     When times the two numbers.
     Then the result should be display.
